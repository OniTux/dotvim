Installation:

    git clone git://github.com/OniTux/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

Compile command-t:

    cd ~/.vim/bundle/command-t
    rake make
